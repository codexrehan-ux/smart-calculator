// script.js\n\n// Add this at the top of your script.js file\ndocument.addEventListener('DOMContentLoaded', () => {\n    const display = document.getElementById('display'); // Assuming there's a display element\n\n    function calculate(expression) {\n        try {\n            const result = eval(expression); // Be cautious with eval in production code\n            return result;\n        } catch (error) {\n            return 'Error';\n        }\n    }\n\n    function updateDisplay(value) {\n        display.value = value;\n    }\n\n    function handleButtonClick(value) {\n        if (value === '=') {\n            const result = calculate(display.value);\n            updateDisplay(result);\n        } else {\n            updateDisplay(display.value + value);\n        }\n    }\n\n    // Keyboard support\n    document.addEventListener('keydown', (event) => {\n        if (event.key >= '0' && event.key <= '9' || ['+', '-', '*', '/'].includes(event.key)) {\n            updateDisplay(display.value + event.key);\n        }\n        if (event.key === 'Enter') {\n            const result = calculate(display.value);\n            updateDisplay(result);\n        }\n    });\n});\n\n// The rest of your existing script.js code\n